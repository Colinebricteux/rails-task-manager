<div class="main-container">
  <h1>The ugly TO DO manager 💩</h1>

  <h3>⬇️ Here's your boring list 🤓 ⬇️</h3>

  <div class="tasks"><% @tasks.each do |t| %>
    <p class="task"> 〰️
      <%= t.completed ? "✅" : "🟥" %>
      <%= link_to "#{t.title}", task_path(t) %>
      <%= link_to "❌", task_path(t), method: :delete, data: { confirm: "Are you sure?" } %>
      </p>
    <% end %>
  </div>

  <p><%= link_to "➕ CREATE A NEW TASK", tasks_new_path %></p>

  <h3>👹 Hurry up ! 💀</h3>
</div>

